# NGINX MCP Server Configuration
# Copy this file to .env and modify values as needed
# These variables will override the values in config.json

# NGINX server host (default: localhost)
NGINX_HOST=localhost

# NGINX server port (default: 8080)
NGINX_PORT=8080

# Project directory for Docker Compose operations
# PROJECT_DIR=c:\Users\Wild Tinker\Documents\source\mcp-server-prototype

# Server configuration
# SERVER_NAME=NGINX Tools
# SERVER_VERSION=1.0.0

# HTTP request timeout in milliseconds (default: 5000)
# HTTP_TIMEOUT=5000

# Optional: Node environment
NODE_ENV=production
